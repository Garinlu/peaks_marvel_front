export class Character {
  id: number;
  name: string;
  thumbnail: string;
  /* Attributes above are only known when getting detail on a character */
  description: string;
  comicsNumber: number;
}
